import numpy as np 
from scipy.optimize import minimize

def MLE(dataframe, parameters):
    """
    --- Input 
    dataframe = the corresponding dataframe 
    parameters = parameters to optimized
    --- Return 
    optimized parameters 
    """
    

